# `<project-name>` — Changelog

**Owner:** Analyst
**Updated at each release.**

---

## `<component>-v<semver>` — `<YYYY-MM-DD>`

- `<shipped item — cross-refs DECISIONS slug and/or B-xxx as appropriate; no rationale>`

---

*Written by Analyst at release-marking Task closure, together with the tag suggestion (see `WAY_OF_WORKING.md` § Task closure). PO commits. Per-task detail lives in branch commits. Complement to `ROADMAP.md`'s Past section — `ROADMAP` is product-facing summary, `CHANGELOG` is implementation detail.*
