# `<project-name>` — Technical Context (`AGENTS.md`)

**Owner:** DEV
**Updated:** `<YYYY-MM-DD>`

Skeleton for a project's `AGENTS.md` — the technical-context file an agent session reads to orient on the codebase.

---

## Architecture overview

`<High-level architecture description — components, data flow, deployment model.>`

---

## Tech stack

`<Per-surface technology choices — language, framework, key libraries, runtime versions.>`

---

## Key files

`<Critical files and their purpose — entry points, config files, files that are surprisingly important.>`

---

## Known gotchas

`<Hidden constraints, workarounds, subtle invariants, behavior that would surprise a reader.>`

---

## Conventions

`<Project-specific conventions that don't fit elsewhere — naming patterns, data formats, import style.>`
